// Make 4 buttons with pics of crystals

// Display random number between 19-120 at beginning of each new game

// Display Wins & Losses

// Each crystal has its own random number value between 1-12

// Display Player Score

// When crystal is clicked, add its num value to Player Score

// Trigger a Win if Player Score === Random Number

// Trigger a Loss if Player Score > Random Number

// After a Win or Loss, restart game and reassign random number value to Random Number & crystal buttons

// Player Score resets to 0 
